[project]
name = "gnuplex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygit2>=1.20.0",
    "structlog>=26.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.8",
]
